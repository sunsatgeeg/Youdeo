<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.geeg.youdeo.video.mapper.VideoMapper">

	<insert id="create" parameterType="java.util.Map">
		insert into video(v_no, v_title, v_description, v_category, u_id) 
		values(video_v_no_SEQ.nextval, #{v_title}, #{v_description}, #{v_category}, #{u_id})
	</insert>
	 
	<select id="findVideo" parameterType="java.lang.Integer"
		resultType="com.geeg.youdeo.video.Video">
		select * from video
		where v_no = #{v_no}
	</select>
	
	<select id="searchVideoList" parameterType="java.lang.String" resultType="com.geeg.youdeo.video.Video">
		select * from video
		where v_title LIKE '%'||#{v_title}||'%'
	</select>
	
	<select id="findVideoList" resultType="com.geeg.youdeo.video.Video">
		select * from video
	</select>
	
	<update id="update" parameterType="com.geeg.youdeo.video.Video">
		update video
		set v_title=#{v_title}, v_description=#{v_description}, v_category=#{v_category}
		where v_no=#{v_no}
	</update>
	
	<delete id="remove" parameterType="java.lang.Integer">
		delete video where
		v_no=#{v_no}
	</delete>

</mapper>